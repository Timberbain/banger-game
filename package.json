{
  "name": "banger-game",
  "version": "1.0.0",
  "private": true,
  "scripts": {
    "format": "npx prettier --write .",
    "format:check": "npx prettier --check ."
  },
  "devDependencies": {
    "prettier": "^3.8.1"
  }
}
