---
phase: 07-hd-viewport-camera
plan: 06
type: execute
wave: 4
depends_on: ["07-03", "07-04", "07-05"]
files_modified: []
autonomous: false

must_haves:
  truths:
    - "Game launches at 1280x720 and pixel art renders crisp without blurring"
    - "Camera smoothly follows the local player with deadzone"
    - "Camera stops at world edges -- no black void visible"
    - "All scenes (Boot, Lobby, Victory, Help, HUD) display correctly at 1280x720"
    - "ARENA bounds are dynamic from map metadata"
  artifacts: []
  key_links: []
---

<objective>
Verify the complete HD viewport and camera system works correctly across all scenes. Run both server and client, test all scenes, camera behaviors, and sprite rendering.

Purpose: Visual verification that all 5 phase success criteria are met before the phase is considered complete.
Output: Verified working system or list of issues to fix.
</objective>

<execution_context>
@/Users/jonasbrandvik/.claude/get-shit-done/workflows/execute-plan.md
@/Users/jonasbrandvik/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/07-hd-viewport-camera/07-RESEARCH.md
@.planning/phases/07-hd-viewport-camera/07-04-SUMMARY.md
@.planning/phases/07-hd-viewport-camera/07-05-SUMMARY.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Compile and startup verification</name>
  <files></files>
  <action>
1. Run `cd server && npx tsc --noEmit` to verify server TypeScript compiles
2. Run `cd client && npx tsc --noEmit` (or equivalent) to verify client TypeScript compiles
3. Start server: `cd server && npm run dev`
4. Start client: `cd client && npm run dev`
5. Verify both start without errors
6. Check browser console for any Phaser errors on load
  </action>
  <verify>
- Server compiles and starts on port 2567
- Client compiles and starts on port 8080
- No TypeScript errors
- No runtime errors in browser console
  </verify>
  <done>Both server and client compile and run without errors.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 2: Visual verification of HD viewport and camera system</name>
  <files></files>
  <action>
Human visual verification of the complete HD Viewport and Camera system.

What was built:
1. Game canvas at 1280x720 with 2x pixel art scaling via camera zoom
2. Camera follow with deadzone, look-ahead, and world edge clamping
3. Match-start overview animation (1.5s arena view then zoom to player)
4. Paran speed zoom-out at high velocity
5. Camera shake on wall impact and damage
6. Death spectator camera with Tab cycling
7. All scenes (Boot, Lobby, Victory, Help, HUD) at 1280x720
8. 2x character sprites with 36-frame animations including idle breathing
9. Dynamic arena bounds in physics

How to verify:

Start both servers:
1. Terminal 1: `cd server && npm run dev`
2. Terminal 2: `cd client && npm run dev`
3. Open http://localhost:8080

Boot Scene: Game window is 1280x720, title screen fills window, text centered.
Lobby Scene: All elements visible and centered, character panels spaced, buttons clickable.
Game Scene (3 tabs): Overview shows arena then zooms in, camera follows with deadzone, no black void at edges, sprites crisp, look-ahead visible, Paran speed zoom-out, camera shake on wall/damage.
HUD: Timer top-center, ping top-right, health bottom, kill feed top-right, no overlap.
Spectator: Camera follows closest alive player, Tab cycles, banner visible.
Victory: Overlay fills 1280x720, stats centered.
Help: Panel fills screen, content readable.
  </action>
  <verify>All visual checks pass with no mispositioned elements, no black void, no sprite corruption.</verify>
  <done>Human approves the HD viewport and camera system across all scenes.</done>
</task>

</tasks>

<verification>
All 5 phase success criteria verified:
1. Game at 1280x720 with crisp pixel art
2. Camera follow with deadzone
3. Camera stops at world edges
4. All scenes correct at 1280x720
5. Dynamic arena bounds
</verification>

<success_criteria>
Human confirms all visual and interactive elements work correctly at 1280x720 across all scenes. No visual corruption, mispositioned elements, or camera issues observed.
</success_criteria>

<output>
After completion, create `.planning/phases/07-hd-viewport-camera/07-06-SUMMARY.md`
</output>
