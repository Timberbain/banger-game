import Phaser from 'phaser';

/**
 * HelpScene - Controls/help overlay scene.
 * Stub for now -- full implementation in Plan 06.
 */
export class HelpScene extends Phaser.Scene {
  constructor() {
    super({ key: 'HelpScene' });
  }

  create() {
    // Will be populated in Plan 06
  }
}
